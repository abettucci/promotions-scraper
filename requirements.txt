# Browser automation
playwright==1.56.0
playwright-stealth==1.0.6
setuptools>=65.0.0  # Requerido por playwright-stealth (pkg_resources)

# Adaptive scraping (Scrapling) - requiere lxml>=6.0.2
scrapling[fetchers]>=0.4.0

# HTML parsing
# NOTA: Scrapling requiere lxml>=6.0.2, así que no fijamos versión
beautifulsoup4>=4.12.0
lxml>=6.0.2

# Environment & config
python-dotenv==1.0.0

# Dashboard
streamlit==1.29.0
pandas==2.1.4
plotly==5.18.0

# HTTP requests
requests==2.31.0

# Database
sqlalchemy==2.0.23

# API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# AI extraction (opcional - requerido para --ai mode)
anthropic>=0.18.0

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

